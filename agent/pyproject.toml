[project]
name = "autoresearch-agent"
version = "0.1.0"
description = "Autoresearch training agent — character-level Transformer on TinyShakespeare"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24.0",
    "requests>=2.28.0",
    "torch>=2.1.0",
    "wandb>=0.19.0",
]
